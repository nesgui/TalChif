{% extends 'layout/dashboard.html.twig' %}

{% set section = 'admin' %}

{% block title %}Dashboard admin{% endblock %}

{% block dashboard_title %}Back-office admin{% endblock %}

{% block dashboard_content %}
<p class="page-subtitle">Bienvenue dans l'espace d'administration TalChif.</p>

<div class="grid">
    <section class="card">
        <h2 class="card-title">Accès rapides</h2>
        <div class="card-actions">
            <a class="btn btn-primary" href="{{ path('admin.user.index') }}">Gérer les utilisateurs</a>
            <a class="btn btn-secondary" href="{{ path('admin.commande.index') }}">Commandes &amp; paiements</a>
            <a class="btn" href="{{ path('accueil') }}">Retour au site</a>
        </div>
    </section>
</div>
{% endblock %}
