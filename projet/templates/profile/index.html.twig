{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/error-handling.css') }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/form-validator.js') }}" defer></script>
{% endblock %}

{% block body %}
<section class="profile-shell">
    <header>
        <h1 class="page-title">Mon profil</h1>
        <p class="page-subtitle">Complétez vos informations et sécurisez votre espace client.</p>
    </header>

    {% if profileIncomplete %}
        <section class="card" style="border-left: 4px solid var(--couleur-warning);">
            <h2 class="card-title">Complétez votre espace client</h2>
            <p class="card-meta">Vos billets restent envoyés par email. Pour les consulter dans votre espace client, ajoutez votre nom complet, votre téléphone et votre mot de passe.</p>
        </section>
    {% endif %}

    <div class="grid">
        <section class="card">
            <h2 class="card-title">Informations</h2>
            <p><strong>Email:</strong> {{ app.user.email }}</p>
            <p><strong>Type de compte:</strong> {{ app.user.role }}</p>
            {{ form_start(infoForm, {'attr': {'class': 'auth-form'}}) }}
                {% include 'partials/form_group_enhanced.html.twig' with {
                    form: infoForm.nom,
                    label: 'Nom complet',
                    placeholder: 'Votre nom complet'
                } %}
                {% include 'partials/form_group_enhanced.html.twig' with {
                    form: infoForm.telephone,
                    label: 'Téléphone',
                    placeholder: '+235 63 00 00 00',
                    help: 'Numéro utilisé pour les notifications'
                } %}
                <div class="card-actions">
                    <button class="btn btn-primary" type="submit">Enregistrer</button>
                </div>
            {{ form_end(infoForm) }}
        </section>

        <section class="card">
            <h2 class="card-title">Sécurité</h2>
            {% if profileIncomplete %}
                <p class="card-meta">Définissez maintenant votre premier mot de passe pour verrouiller votre espace client.</p>
            {% endif %}
            {{ form_start(passwordForm, {'attr': {'class': 'auth-form'}}) }}
                {% include 'partials/form_group_enhanced.html.twig' with {
                    form: passwordForm.current_password,
                    label: 'Mot de passe actuel',
                    placeholder: 'Laissez vide si vous n\'avez pas encore défini de mot de passe'
                } %}
                {% include 'partials/form_group_enhanced.html.twig' with {
                    form: passwordForm.new_password,
                    label: 'Nouveau mot de passe',
                    placeholder: 'Au moins 6 caractères'
                } %}
                {% include 'partials/form_group_enhanced.html.twig' with {
                    form: passwordForm.new_password_confirm,
                    label: 'Confirmer le nouveau mot de passe',
                    placeholder: 'Répétez le mot de passe'
                } %}
                <div class="card-actions">
                    <button class="btn btn-primary" type="submit">Changer le mot de passe</button>
                </div>
            {{ form_end(passwordForm) }}
        </section>
    </div>
</section>
{% endblock %}

